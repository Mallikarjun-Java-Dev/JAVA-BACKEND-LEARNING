package SDA_ARRAY;

class Employee {
	private String name;
	private int age;
	private char gender;

	public Employee(String name, int age, char gender) {
		this.name = name;
		this.age = age;
		this.gender = gender;
	}

	// Getter for name (IMPORTANT for search)
	// This allows us to access the name of the employee when searching
	public String getName() {
		return name;
	}

	public String toString() {
		return "Employee[name=" + name + ", age=" + age + ", gender=" + gender + "]";
	}
}

class Details {
	Employee[] arr;

	public Details(int size) {
		arr = new Employee[size];
	}

	public void insertion(int index, Employee emp) {
		try {
			if (arr[index] == null) {
				arr[index] = emp;
				System.out.println("data inserted successfully");
			} else {
				System.out.println("data already exist!");
			}
		} catch (Exception e) {
			System.out.println("Invalid index");
		}
	}

	public void traverse() {
		for (int i = 0; i < arr.length; i++) {
			System.out.println(arr[i]);
		}
	}

	public void search(String name) {
		boolean found = false;

		for (int i = 0; i < arr.length; i++) {
			if (arr[i] != null && arr[i].getName().equals(name)) {// Check if the current employee
				// is not null and matches the search name
				System.out.println("found at index: " + i);
				found = true;
			}
		}

		if (!found) {
			System.out.println("data is not present");
		}
	}
	public void delete(String name) {
		boolean found=false;
		for(int i=0;i<arr.length;i++) {
			if(arr[i]!=null && arr[i].getName().equals(name)) {
				
			}
		}
	}
}

public class Mainclass1 {
	public static void main(String[] args) {

		Details d = new Details(5);

		d.insertion(0, new Employee("Mallikarjun", 22, 'M'));// 0
		d.insertion(1, new Employee("Suman", 21, 'F')); // 1
		d.insertion(2, new Employee("Satyarth22", 22, 'M'));// 2
		d.insertion(3, new Employee("Satyarth", 22, 'M'));// 3
		d.insertion(4, new Employee("Satyarth", 22, 'M'));// 4

		// d.traverse();

		d.search("Satyarth22");
	}
}